--- a/scripts/homo_sapiens_composite_mapping.py
+++ b/scripts/homo_sapiens_composite_mapping.py
@@ -370,7 +370,7 @@
             counters["exons"] += 1
         elif feature_type not in TRANSCRIPT_EXCLUDED_TYPES and (
             annotation != "ncbi" or feature_type in SIMULATOR_FEATURE_TYPES
-        ):
+        ) and (annotation != "gencode" or feature_type == "transcript"):
             model_id, source_id, parents = _annotation_id(annotation, feature_type, attributes)
             if not model_id:
                 continue
